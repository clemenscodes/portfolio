import { Education } from './education';
import type { Meta } from '@storybook/react';

const Story: Meta<typeof Education> = {
    component: Education,
    title: 'Education',
};
export default Story;

export const Primary = {
    args: {},
};
