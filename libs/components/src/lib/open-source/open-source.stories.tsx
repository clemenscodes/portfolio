import { OpenSource } from './open-source';
import type { Meta } from '@storybook/react';

const Story: Meta<typeof OpenSource> = {
    component: OpenSource,
    title: 'OpenSource',
};
export default Story;

export const Primary = {
    args: {},
};
