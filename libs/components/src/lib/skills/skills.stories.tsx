import type { Meta } from '@storybook/react';
import { Skills } from './skills';

const Story: Meta<typeof Skills> = {
    component: Skills,
    title: 'Skills',
};
export default Story;

export const Primary = {
    args: {},
};
